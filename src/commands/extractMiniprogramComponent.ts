import { workspace, ExtensionContext, extensions, env, commands, window, Selection, Position, Hover } from 'vscode';

export async function extractMiniprogramComponent(folderPath: any): Promise<void> {
  // folderPath;
  // const editor = window.activeTextEditor;
  //     //类型转换
  // const selection = editor?.selections.find((selection) => {
  //     return !selection.isEmpty;
  // });
  // selection;
}